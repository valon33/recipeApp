import React from "react";
import Logo from "../Logo/Logo";

const Header = () => {
  return (
    <div style={{ display: "flex" }}>
      <Logo />
      <h1>Header</h1>
    </div>
  );
};

export default Header;
